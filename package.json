{
  "name": "sure-app-mobile",
  "private": true,
  "description": "SureLink — API runs from backend/. Works on Render with or without Root Directory set.",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}
